import { atom } from "nanostores";

export const loadingSimulateTxAtom = atom<boolean>(false);

export const loadingWalletDataAtom = atom<boolean>(false);

export const loadingStatsAtom = atom<boolean>(false);
